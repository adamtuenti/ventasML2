import { FiltroProductoLocalPipe } from './filtro-producto-local.pipe';

describe('FiltroProductoLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProductoLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
