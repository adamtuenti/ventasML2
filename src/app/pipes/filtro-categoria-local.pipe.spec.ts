import { FiltroCategoriaLocalPipe } from './filtro-categoria-local.pipe';

describe('FiltroCategoriaLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCategoriaLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
